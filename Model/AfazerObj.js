export default class AfazerObj {

    constructor(descricao, data, prioridades, marcado) {
        this.descricao = descricao
        this.data = data
        this.prioridades = prioridades
        this.marcado = marcado
    }
}